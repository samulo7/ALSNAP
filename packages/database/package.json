{
  "name": "@alisnap/database",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "db:check": "tsx src/check-connectivity.ts",
    "lint": "tsc --project tsconfig.json --pretty false --noEmit",
    "typecheck": "npm run lint"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "prisma": "^6.6.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
